export enum ButtonVariants {
  Default = 'default',
  Primary = 'primary',
  Secondary = 'secondary'
}

export enum TextFieldVariants {
  Standard = 'standard',
  Outline = 'outline',
  Error = 'error'
}
