import { get, post, update, remove } from '..';

import fetch from 'jest-fetch-mock';

describe('Testing API', () => {
  beforeEach(() => {
    fetch.resetMocks();
  });

  test('should load product data', () => {});

  test('should create data', () => {});

  test('should update data', () => {});

  test('should should remove data', () => {});
});
